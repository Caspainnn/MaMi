# 数据表

## 用户信息 User

| 字段 | 说明 |
| --- | --- |
| user_id | 用户唯一ID |
| openid | 微信OpenID |
| 昵称 | 微信昵称 |
| 头像 | 微信头像 |
| 注册时间 | 首次登录 |
| 连续打卡天数 | Streak |
| 累计刷题数 | 统计 |
| 累计斩杀数 | 统计 |
| AI剩余额度 | 每日刷新 |

## 用户题目 UserProblem

| 字段 | 说明 |
| --- | --- |
| id | 唯一ID |
| user_id | 用户 |
| problem_id | 题目 |
| 刷题次数 | 统计 |
| 掌握等级 | 枚举，共有Lv0-Lv5六个等级，详见【配置表-学习状态标签配置】 |
| 上次刷题时间 last_practiced_at | 最近一次成功保存提交记录的时间 |
| 预计复刷时间 next_review_at | 由状态行为表计算；首页据此筛选到期复刷题 |
| 遗忘次数 forget_count | 本次回忆判定为“遗忘回忆”时加 1，用于复刷排序 |
| 当日升级日期 last_level_up_date | 用户本地自然日；用于限制同一题每日最多升级一次 |
| 版本号 version | 乐观锁版本，避免重复请求覆盖状态 |
| 是否斩杀 is_slashed | 用户主动确认“完全掌握、不再复刷”时为 `true` |
| 斩杀时间 slashed_at | 用户点击斩杀并确认的时间，可为空 |

约束：`(user_id, problem_id)` 唯一。一条 PracticeRecord 可以重复提交，但每条记录保存后都必须独立关联一次 AIAnalysis；同一题同一自然日最多提升一个等级，降级和下次复刷时间仍按每条提交记录计算。

## 题库 QuestionBank

| 字段 | 说明 |
| --- | --- |
| question_bank_id | 题库唯一 ID |
| 名称 | 例如 LeetCode Hot 100、代码随想录 |
| 描述 | 题库简介 |
| 状态 | 启用 / 停用 |
| 创建时间 | 时间 |

## 题库题目关联 QuestionBankProblem

| 字段 | 说明 |
| --- | --- |
| id | 唯一 ID |
| question_bank_id | 关联 QuestionBank |
| problem_id | 关联 Problem |
| sort_order | 题库内固定顺序；新刷按该字段升序推荐 |

约束：`(question_bank_id, problem_id)` 唯一。

## 斩杀计划 StudyPlan

| 字段 | 说明 |
| --- | --- |
| plan_id | 唯一 ID |
| user_id | 用户 |
| question_bank_id | 选择的题库 |
| plan_type | `by_days` / `by_daily_new_count` |
| target_days | 按天数规划时填写 |
| daily_new_count | 按每日新刷数量规划时填写 |
| estimated_daily_new_count | 创建时计算的预估值 |
| estimated_daily_review_count | 创建时计算的预估值 |
| estimated_finish_date | 创建时计算后展示；后续不动态预测 |
| status | `active` / `reset` / `covered` |
| created_at | 创建时间 |
| reset_at | 重置时间，可为空 |

规则：每个用户至多一个 `active` 计划；创建新计划时，旧计划改为 `covered`，新计划立即启用。重置计划时，当前计划标记为 `reset`，清空首页任务展示，用户可重新创建计划；历史 PracticeRecord 不删除。

## 计划题目关联 PlanProblem

| 字段 | 说明 |
| --- | --- |
| id | 唯一 ID |
| plan_id | 关联 StudyPlan |
| problem_id | 关联 Problem |
| plan_order | 创建计划时按题库顺序写入 |
| initial_status | 初始状态，默认 Lv0 |

约束：`(plan_id, problem_id)` 唯一。首页“今日新刷”只从当前计划的 PlanProblem 中选择。



## 每日情况统计

| 字段 | 说明 |
| --- | --- |
| date |  |
| user_id | 用户 |
| 新刷提交数 |  |
| 复刷提交数 |  |
| 完成状态 | 未完成/部分完成/完成 |

## 刷题记录 PracticeRecord

| 字段 | 说明 |
| --- | --- |
| record_id | 唯一ID |
| problem_id | 题目 |
| user_id | 用户 |
| 提交代码 | 代码 |
| 总结 | 文本 |
| 耗时 | 枚举，详见【配置表-耗时说明】 |
| 提交结果 | 枚举，详见【配置表-刷题状态配置】 |
| 提交时间 | 时间 |

## AI分析记录 AIAnalysis

| 字段 | 说明 |
| --- | --- |
| analysis_id | 唯一ID |
| record_id | 关联刷题记录 |
| 总结（第一段） | 文本 |
| 扩展（第二段） | 文本 |
| Prompt版本 | 方便后续升级，当前为0 |

## 题目信息 Problem

| 字段 | 说明 |
| --- | --- |
| problem_id | 题目ID |
| 题号 | Leetcode编号 |
| 标题 | 题目名称 |
| 难度 | 简单/中等/困难 |
| 标签 | DP、DFS等 |
| 简介 | 文本 |
| 链接 | 跳转地址 |
| 所属分类 |  |
| 所属题库 | 题库id |
