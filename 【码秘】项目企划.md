# 【码秘】项目企划

# 一、产品定位

## 产品名称

码秘（CodeMate）：全称 代码秘书

## Slogan

**像背单词一样刷算法，告别刷完就忘。**

## Logo

![logo](e:\Personal\Desktop\26年临时\码秘\相关文档\码秘原型图\logo.png)

---

## 产品定位

**码秘是一款面向算法面试场景的刷题记忆管理工具。**

区别于LeetCode、牛客等在线OJ平台，码秘不负责在线做题，而是专注于用户完成刷题后的复盘、记忆管理和长期掌握。

产品以"间隔重复（Spaced Repetition）"为核心，通过学习计划调度、刷题记录沉淀、AI学习教练和游戏化激励等能力，帮助用户持续提升算法掌握程度，实现从"会做一道题"到"真正掌握一道题"的转变。

---

## 产品核心价值

> **帮助用户真正掌握算法，而不是刷完算法。**

## 产品关键词

```Plain Text
Hot100-刷题记录-间隔重复-AI学习秘书-长期掌握-斩杀计划
```

## 产品边界

负责：

✅ 学习计划

✅ 记忆管理

✅ AI复盘

✅ 数据统计

不负责：

❌ 在线IDE

❌ 在线判题

❌ Leetcode同步

# 二、产品框架

"e:\Personal\Desktop\26年临时\码秘\相关文档\【码秘】骨架梳理表.xlsx"



# 四、原型预览

"e:\Personal\Desktop\26年临时\码秘\相关文档\码秘原型图"

# 五、算法逻辑

## 斩杀计划

### 1\.1 根据天数完成

输入：规定完成的天数 x，题目总数=N

输出：预计每天新学（study\_num）、复习数量（review\_num）

要求：根据记忆遗忘曲线，每道题一共刷5遍才结束（当天新学，复习间隔1,2,7,15）

伪代码：

```Python
def calculate_plan(x):
    if x < 16:
        return "Error: 至少需要16天"
    
    D = x - 15  # 有效新学天数
    study = [0] * (x + 1)  # 下标从1开始
    review = [0] * (x + 1)
    
    # 1. 分配每日新学 (保证总和100)
    remaining = 100
    for i in range(1, D + 1):
        days_left = D - i + 1
        num = (remaining + days_left - 1) // days_left  # 向上取整
        study[i] = num
        remaining -= num
    
    # 2. 统计每日复习 (间隔 1,2,7,15)
    for i in range(1, x + 1):
        total = 0
        for offset in [1, 2, 7, 15]:
            prev_day = i - offset
            if 1 <= prev_day <= D:  # 只统计有新学的那天
                total += study[prev_day]
        review[i] = total
    
    return study, review
```

### 1\.2 根据刷题数量完成

输入：规定每天新学题目数量 x，题目总数=N

输出：预计复习数量（review\_num）、预计花费天数（cost\_time）

要求：根据记忆遗忘曲线，每道题一共刷5遍才结束（当天新学，复习间隔1,2,7,15）

伪代码：

```Python
import math

def calculate_plan(x):
    N = 100
    # 1. 计算学习阶段天数 L
    L = math.ceil(N / x)
    
    # 2. 总花费天数
    cost_time = L + 15
    
    # 3. 生成每日新学数量数组（下标从 1 开始）
    study = [0] * (L + 1)  # 索引 1..L
    remaining = N
    for day in range(1, L + 1):
        study[day] = min(x, remaining)
        remaining -= study[day]
    
    # 4. 计算每日复习数量数组（下标从 1 到 cost_time）
    review = [0] * (cost_time + 1)
    offsets = [1, 2, 7, 15]
    
    for day in range(1, cost_time + 1):
        total = 0
        for off in offsets:
            prev = day - off
            if 1 <= prev <= L:   # 只有那天有新学任务才产生复习
                total += study[prev]
        review[day] = total
    
    # 5. 返回结果（复习数组 + 总天数）
    return review[1:], cost_time
```



## AI 总结

输入：用户提交的 代码（可能没有）、刷题状态、耗时、历史提交记录（历史刷题状态\+耗时）、题目及其介绍

有代码\-prompt：

```Plain Text
你是一名算法训练复盘助手，负责根据用户本次刷题记录，生成简短、高价值的复盘总结。
你的目标不是重新讲题，而是：
1. 判断用户当前掌握程度；
2. 找出代码中的真实问题；
3. 给出针对性的改进建议；
4. 提供积极反馈，帮助用户建立算法记忆。

用户输入信息：
【题目信息】
{{problem_info}}

【用户代码】
{{user_code}}

【本次刷题状态】
{{current_status}}

状态可能包括：
- 鼠鼠不会：完全没有思路，无法独立开始写代码
- 有点印象：能够回忆出主要思路，但无法独立完成代码
- 差一捏捏：基本完成实现，但存在 Bug、边界、复杂度等问题（对应 WA / CE / RE / TLE 等）
- 独立斩杀：无需借助外部帮助，独立完成并 AC

【本次耗时】
{{current_time}}

耗时可能包括：
<5 分钟：非常熟悉，几乎形成肌肉记忆
5~15 分钟：正常回忆速度
15~30 分钟：需要一定思考
>30 分钟：回忆困难

【历史刷题记录】
{{history_records}}

历史记录只包含：
- 历史状态
- 历史耗时

请严格按照以下规则输出：
========================

第一段：
输出【一句极短总结】，要求：

1. 直接指出核心问题或者给予反馈；
2. 必须包含时间复杂度和空间复杂度；
3. 长度控制在50字以内；
4. 不要复述题目；
5. 不要输出泛泛建议。

情况判断：

如果代码存在问题：
- 直接指出错误原因；
- 指明错误位置或错误思想；
- 给出一句修改方向。

例如：
“哈希判断逻辑写反了，导致重复元素漏判，改为记录出现状态即可。O(n)，空间O(n)。”

如果代码正确：
根据用户状态给予情绪反馈：

例如：
“5分钟独立AC，太猛了，这题已经吃透。双指针解法优秀，O(n)，空间O(1)。”

可以使用轻微口语：
- 牛逼
- 稳
- 狠狠拿下
- 这题已经斩杀

但不要夸张。
========================
第二段：
只有满足以下情况才输出：
1. 用户代码存在逻辑问题；
2. 用户当前解法不是最优解；
3. 存在明显优化空间。

否则不要输出第二段。


第二段要求：
针对用户当前代码进行解释：
如果代码错误：
说明：
- 错误原因；
- 正确思考方向；
- 如何修改。

禁止输出完整题解。
如果代码正确但不是最优：
说明：
- 当前方法的问题；
- 更优方法的核心思想；
- 优化后的复杂度。

控制在100字以内。
========================
禁止：
- 不要生成完整题解；
- 不要输出代码；
- 不要长篇解释；
- 不要评价用户“基础差”等负面内容；
- 不要根据历史记录虚构用户行为。
```

无代码\-prompt：

```Plain Text
你是「码秘」算法记忆复盘助手。

你的任务：根据用户刷题记录，评估用户对该算法的记忆状态，而不是讲解题目或生成题解。

分析依据：
1. 回忆结果：反映算法模型是否能被提取；
2. 耗时：反映提取速度和熟练程度；
3. 历史记录：判断记忆是否稳定；
4. 题目信息：辅助给出复习方向。

输入：

【题目信息】
{{problem_info}}

【本次回忆结果】
{{recall_result}}

取值：
- 鼠鼠不会：完全没有思路，无法独立开始写代码
- 有点印象：能回忆主要思路，但无法独立完成代码
- 差一捏捏：基本完成实现，但存在 Bug、边界、复杂度等问题
- 独立通过：无需外部帮助，独立完成并 AC

【本次耗时】
{{time_cost}}

耗时标准：
-<5分钟：非常熟悉，接近肌肉记忆
- 5~15分钟：正常回忆速度
- 15~30分钟：需要一定思考
- >30分钟：回忆困难

【历史复习记录】
{{history_records}}

输出两段：

第一段：
生成一句50字以内的记忆状态总结。

要求：
- 直接评价当前掌握程度；
- 不讲题解；
- 不评价代码；
- 不假设用户使用的算法。

判断规则：

🎯 独立通过：
<5分钟：已经形成肌肉记忆，快速提取；
5~15分钟：算法模型稳定掌握；
15~30分钟：会做但熟练度不足；
>30分钟：掌握但记忆不够稳。

🛠 差一捏捏：
说明算法模型已建立，但执行细节不稳定。

🤔 有点印象：
说明记住部分思路，但无法完整调用。

🐭 鼠鼠不会：
说明尚未建立题型与算法模型之间的联系。

允许使用轻量口语：
牛逼、稳、狠狠拿下等。

第二段：
仅在以下情况输出：
- 回忆结果不是🎯独立通过；
- 历史记录出现明显退步；
- 当前耗时明显异常。

内容：
给出100字以内复习建议。

🐭 鼠鼠不会：
说明应该记忆的核心算法模型和识别方式。

🤔 有点印象：
说明已经掌握什么，以及缺少什么。

🛠 差一捏捏：
说明下次重点关注边界、细节或易错点。

历史退步：
提醒记忆衰退，需要调整复习间隔。

禁止：
- 输出完整题解；
- 输出代码；
- 编造用户错误；
- 批评用户能力。
```



## 首页题目展示

说明：本算法在设计如何将题库中的题目状态和用户都计划实际结合，为用户推荐更优质的刷题方案

输入：数据表\-用户题目表、配置表\-档次对照表、配置表\-状态行为表

#### 方案简要介绍

首页题目展示功能基于**用户题目状态 \+ 记忆曲线 \+ 用户学习计划**进行动态推荐。

系统通过用户题目表（UserProblem）记录每道题当前掌握状态，并结合用户每次刷题后的回忆评分结果，动态调整：

1. **题目掌握等级**

    - 根据用户本次复习表现（回忆结果 \+ 耗时）计算回忆得分； 

    - 根据档次对照表判断本次回忆属于： 

        - 优秀回忆 

        - 一般回忆 

        - 遗忘回忆 

    - 根据状态行为表调整题目掌握等级以及预计复刷时间。 

2. **今日新刷推荐**

    - 从用户计划题库中筛选尚未学习（Lv0）的题目； 

    - 根据计划进度、题目优先级、知识点覆盖等规则推荐今日新增题目； 

    - 控制每日新增数量，保证用户持续学习。 

3. **今日复刷推荐**

    - 从用户题目表中筛选达到预计复刷时间的题目； 

    - 根据当前掌握等级、逾期时间、历史表现进行排序； 

    - 优先推荐： 

        - 已到复习时间的题目； 

        - 掌握等级较低的题目； 

        - 曾经出现遗忘回忆的题目； 

    - 形成符合用户当前记忆状态的复习列表。



#### 伪代码

首页推荐生成算法：

```Python
def generate_homepage_problems(user_id):

    # =========================
    # 1. 获取用户学习数据
    # =========================

    user_problems = get_user_problems(user_id)

    study_plan = get_user_plan(user_id)


    today_new = []
    today_review = []


    # =========================
    # 2. 生成今日新刷题目
    # =========================

    # 获取未学习题目
    new_problems = filter(
        user_problems,
        condition = 掌握等级 == "含苞·蓄势待发"
    )


    # 根据用户计划排序
    new_problems = sort(
        new_problems,
        key=[
            计划优先级,
            知识点覆盖,
            题目难度匹配
        ]
    )


    # 控制每日新增数量
    today_new = new_problems[:每日新增数量]


    # =========================
    # 3. 生成今日复刷题目
    # =========================


    # 获取达到复刷时间的题目
    review_candidates = filter(
        user_problems,
        condition =
        预计复刷时间 <= 当前时间
    )


    # 计算复刷优先级

    for problem in review_candidates:

        priority_score = 0


        # 等级越低，优先级越高
        priority_score += (
            最大等级 -
            problem.掌握等级等级
        ) * 10


        # 逾期时间越长，优先级越高
        overdue_days = (
            当前时间 -
            problem.预计复刷时间
        ).days

        priority_score += overdue_days * 5


        # 历史遗忘次数越多，优先级越高
        priority_score += (
            problem.遗忘次数 * 10
        )


        problem.priority = priority_score



    # 按优先级排序

    review_candidates.sort(
        key=priority_score,
        reverse=True
    )


    # 控制每日复刷数量

    today_review = review_candidates[
        :每日复刷数量
    ]


    # =========================
    # 4. 返回首页展示数据
    # =========================

    return {

        "今日新刷": today_new,

        "今日复刷": today_review

    }
```

用户完成复刷后的状态更新算法:

```Python
def update_problem_status(
    user_problem,
    recall_result,
    cost_time
):

    # =========================
    # 1. 计算本次得分
    # =========================

    status_score = get_recall_score(
        recall_result
    )


    time_score = get_time_score(
        cost_time
    )


    total_score = (
        status_score
        +
        time_score
    )


    # =========================
    # 2. 判断回忆档次
    # =========================

    if total_score >= 60:

        grade = "优秀回忆"


    elif total_score >=20:

        grade = "一般回忆"


    else:

        grade = "遗忘回忆"



    # =========================
    # 3. 查询状态行为规则
    # =========================

    behavior = get_behavior(
        当前等级=user_problem.掌握等级,
        回忆档次=grade
    )


    # =========================
    # 4. 更新等级
    # =========================

    user_problem.掌握等级 = (
        behavior.目标等级
    )


    # =========================
    # 5. 更新复刷时间
    # =========================

    user_problem.预计复刷时间 = (
        当前时间
        +
        behavior.复习间隔
    )


    # 更新刷题次数

    user_problem.刷题次数 += 1

    user_problem.上次刷题时间 = 当前时间


    save(user_problem)
```



